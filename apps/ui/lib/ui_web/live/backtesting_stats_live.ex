defmodule UiWeb.BacktestingStatsLive do
  @moduledoc false

  use Phoenix.LiveView
  require Logger

  def render(assigns) do
    ~L"""

    """
  end

  def mount(%{}, socket) do
    {:ok, socket}
  end
end
